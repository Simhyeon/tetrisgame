#[derive(Default)]
pub struct Blockage {
    pub axis_right : bool,
    pub axis_left : bool,
    pub axis_down : bool,
    pub rotate_right : bool,
    pub rotate_left : bool,
    pub shoot: bool,
}
